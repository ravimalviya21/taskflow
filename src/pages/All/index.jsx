import { TaskDashboard } from '../../components';

const All = () => (
  <TaskDashboard
    title="All tasks"
    subtitle="View, filter, and manage every task in one place."
  />
);

export default All;
